/**
 * Google Maps API constants
 *
 * Keep library array as a stable reference to avoid LoadScript reload warnings.
 * @see https://react-google-maps-api-docs.netlify.app/docs/use-js-api-loader
 */

export const GOOGLE_MAPS_LIBRARIES: ("places")[] = ["places"];
